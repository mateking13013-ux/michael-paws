---
// HeroSection component - Banner background with text left, image right
---

<section class="hero">
  <div class="hero-banner">
  <div class="hero-container">
    <div class="hero-content">
      <h1 class="hero-title">
        Premium<br>
        <span class="highlight">Staffie</span><br>
        Puppies
      </h1>
      <p class="hero-description">
        Lovingly raised Staffordshire Bull Terrier puppies from our family to yours. Health guaranteed, fully vaccinated, and ready to steal your heart. We provide lifetime support for every puppy.
      </p>
      <a href="/available-puppies" class="btn-primary">View Puppies</a>

      <div class="hero-contact">
        <div class="contact-item">
          <span class="contact-icon">
            <svg viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>
          </span>
          <span>info@michaelpaws.com</span>
        </div>
      </div>
    </div>

    <div class="hero-image">
      <img src="/images/h1.jpeg" alt="Staffordshire Bull Terrier puppy" />
    </div>
  </div>
  </div>

  <div class="trust-bar">
    <div class="trust-container">
      <div class="trust-item">
        <span class="trust-icon">
          <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
        </span>
        <span class="trust-label">Family Raised</span>
      </div>
      <div class="trust-item">
        <span class="trust-icon">
          <svg viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        </span>
        <span class="trust-label">Health Guaranteed</span>
      </div>
      <div class="trust-item">
        <span class="trust-icon">
          <svg viewBox="0 0 24 24"><path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/></svg>
        </span>
        <span class="trust-label">Vet Checked</span>
      </div>
      <div class="trust-item">
        <span class="trust-icon">
          <svg viewBox="0 0 24 24"><path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm-2 16l-4-4 1.41-1.41L10 14.17l6.59-6.59L18 9l-8 8z"/></svg>
        </span>
        <span class="trust-label">Lifetime Support</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    display: block;
    font-family: var(--font-family, 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif);
    overflow: hidden;
    padding: 0;
  }

  .hero-banner {
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .hero-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 5rem 2rem 4rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 3rem;
    min-height: 560px;
  }

  /* Content - left side */
  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-size: 4rem;
    font-weight: 900;
    line-height: 1.05;
    color: var(--color-primary, #1B2A4A);
    margin: 0 0 1.5rem;
    letter-spacing: -1px;
    text-transform: uppercase;
  }

  .highlight {
    position: relative;
    display: inline-block;
  }

  .highlight::after {
    content: '';
    position: absolute;
    left: -10px;
    right: -10px;
    top: -4px;
    bottom: -4px;
    border: 3px solid var(--color-primary, #1B2A4A);
    border-radius: 50px;
    opacity: 0.3;
  }

  .hero-description {
    font-size: 1.1rem;
    line-height: 1.75;
    color: var(--color-gray-700, #616161);
    margin: 0 0 2rem;
    max-width: 520px;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--color-primary, #1B2A4A);
    color: white;
    padding: 1rem 2.5rem;
    border-radius: 8px;
    font-size: 1.05rem;
    font-weight: 700;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    transition: all 0.3s ease;
    box-shadow: 0 4px 16px rgba(27, 42, 74, 0.3);
  }

  .btn-primary:hover {
    background: var(--color-primary-dark, #0F1B33);
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(27, 42, 74, 0.4);
    color: white;
  }

  .hero-contact {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-top: 2rem;
  }

  .contact-item {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-gray-800, #424242);
  }

  .contact-icon {
    width: 28px;
    height: 28px;
    background: var(--color-primary, #1B2A4A);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .contact-icon svg {
    width: 16px;
    height: 16px;
    fill: white;
  }

  /* Image - right side */
  .hero-image {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-image img {
    width: 100%;
    max-width: 500px;
    height: auto;
    border-radius: 16px;
    object-fit: cover;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
  }

  /* Trust bar */
  .trust-bar {
    background: #F0F4F8;
    border-bottom: 1px solid rgba(27, 42, 74, 0.08);
  }

  .trust-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: center;
    gap: 3rem;
    padding: 1.25rem 1.5rem;
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  .trust-icon {
    width: 22px;
    height: 22px;
    flex-shrink: 0;
  }

  .trust-icon svg {
    width: 100%;
    height: 100%;
    fill: var(--color-primary, #1B2A4A);
  }

  .trust-label {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-primary-dark, #0F1B33);
    letter-spacing: 0.2px;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .hero-container {
      grid-template-columns: 1fr;
      padding: 3rem 1.5rem 2rem;
      min-height: auto;
    }

    .hero-content {
      text-align: center;
    }

    .hero-title {
      font-size: 3rem;
    }

    .hero-description {
      max-width: 100%;
      margin-left: auto;
      margin-right: auto;
    }

    .btn-primary {
      margin: 0 auto;
    }

    .hero-contact {
      align-items: center;
    }

    .hero-image {
      display: none;
    }

    .trust-container {
      gap: 1.5rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .trust-label {
      font-size: 0.8rem;
    }
  }

  @media (max-width: 480px) {
    .hero-container {
      padding: 2rem 1rem 1.5rem;
    }

    .hero-title {
      font-size: 2.25rem;
    }

    .hero-description {
      font-size: 1rem;
    }

    .btn-primary {
      padding: 0.9rem 2rem;
      font-size: 0.95rem;
    }

    .trust-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0.75rem;
      padding: 1rem;
    }
  }
</style>
